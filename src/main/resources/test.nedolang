function fact(n) {
    if (n == 0) {
        return 1;
    } else {
        return n * fact(n - 1);
    }
}

print(fact(10));

var arr = [5, 2, 8, 3, 1];
print(arr);

arr[0] = 10;
print(arr[0]);
print(arr[1]);